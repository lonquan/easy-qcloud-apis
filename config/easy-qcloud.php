<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | 腾讯云 API 配置
    |--------------------------------------------------------------------------
    |
    | 此文件包含腾讯云 API 的配置信息，包括认证凭据和服务配置。
    | 支持多套认证凭据配置，便于在不同环境中使用不同的账户。
    |
    */

    /*
    |--------------------------------------------------------------------------
    | 认证凭据配置 (Profiles)
    |--------------------------------------------------------------------------
    |
    | 支持配置多套认证凭据，每个 profile 包含 secret_id 和 secret_key。
    | 可以通过 access_key 参数指定使用哪套凭据。
    |
    */
    'profiles' => [
        'default' => [
            'secret_id' => env('QCLOUD_SECRET_ID'),
            'secret_key' => env('QCLOUD_SECRET_KEY'),
        ],

        // 可以添加更多认证凭据
        // 'production' => [
        //     'secret_id' => env('QCLOUD_PROD_SECRET_ID'),
        //     'secret_key' => env('QCLOUD_PROD_SECRET_KEY'),
        // ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 服务配置 (Services)
    |--------------------------------------------------------------------------
    |
    | 配置各个腾讯云产品的服务参数，包括区域、版本和默认参数。
    | 每个服务可以配置不同的区域和版本。
    |
    */
    'services' => [
        'ocr' => [
            'region' => env('QCLOUD_OCR_REGION', 'ap-beijing'),
            'version' => '2018-05-22',
            'defaults' => [
                // 可以在这里添加 OCR 服务的默认参数
            ],
        ],

        'faceid' => [
            'region' => env('QCLOUD_FACEID_REGION', 'ap-beijing'),
            'version' => '2018-03-01',
            'defaults' => [
                // 可以在这里添加 FaceID 服务的默认参数
            ],
        ],

        'captcha' => [
            'region' => env('QCLOUD_CAPTCHA_REGION', 'ap-beijing'),
            'version' => '2019-07-22',
            'defaults' => [
                // 可以在这里添加 Captcha 服务的默认参数
            ],
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 日志配置
    |--------------------------------------------------------------------------
    |
    | 配置日志记录的相关参数。
    |
    */
    'logging' => [
        'enabled' => env('QCLOUD_LOGGING_ENABLED', true),
        'channel' => env('QCLOUD_LOGGING_CHANNEL', 'default'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 请求配置
    |--------------------------------------------------------------------------
    |
    | 配置 HTTP 请求的相关参数。
    |
    */
    'request' => [
        'timeout' => env('QCLOUD_REQUEST_TIMEOUT', 30),
    ],
];
